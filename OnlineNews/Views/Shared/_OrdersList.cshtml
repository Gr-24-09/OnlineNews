@model List<OnlineNews.Models.ViewModels.OrderViewModel>

<table class="table">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Customer</th>
            <th>Order Date</th>
            <th>Total Cost</th>
            <th>Movies</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.OrderId</td>
                <td>@order.CustomerName</td>
                <td>@order.OrderDate.ToString("yyyy-MM-dd HH:mm")</td>
                <td>@order.Totalcost.ToString("C")</td>
                <td>
                    <ul>
                        @foreach (var item in order.Movies)
                        {
                            <li>@item.ProductName - Quantity: @item.Quantity, Price: @item.Price</li>
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>